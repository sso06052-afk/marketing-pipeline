[phases.setup]
nixPkgs = ["python313", "python313Packages.pip"]

[phases.install]
cmds = ["pip install -r pipeline/requirements.txt"]

[start]
cmd = "cd pipeline && uvicorn server:app --host 0.0.0.0 --port $PORT"
